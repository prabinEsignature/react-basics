const Loader = () => {
  return (
    <div className="fixed top-0 left-0 w-full h-full bg-black/40 z-50 transition ease-in-out duration-100 flex items-center justify-center">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
